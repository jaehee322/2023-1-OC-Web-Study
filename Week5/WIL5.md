HTML5 문서는 반드시 !DOCTYPE html으로 시작하여 문서 형식(document type)을 HTML5로 지정한다. 시맨틱 태그란 브라우저, 검색엔진, 개발자 모두에게 콘텐츠의 의미를 명확히 설명하는 역할을 한다. html,head,title,style,link,script,meta,body 태그 알기. h,b,strong,i,em,small,mark,del,ins,sub/sup,p,br,pre,hr,q,blockquote 태그 알기
HTML5에서는 HTML는 정보와 구조화, CSS는 styling의 정의라는 본연의 임무에 충실한 명확한 구분이 이루어졌다. HTML과 CSS는 각자의 문법을 갖는 별개의 언어이며 HTML은 CSS를 포함할 수 있다. 그러나 HTML없이 단독으로 존재하는 CSS는 의미가 없다. selector,property,value. html에 css를 연동하기 위해: link style, embedding style, inline style. 전체 셀렉터, 태그 셀렉터, ID 셀렉터, 클래스 셀렉터, 어트리뷰트 셀렉터, 복합 셀렉터(후손,자식,형제(동위)), 가상 클래스 셀렉터 등. 크기 단위: cm, mm, inch 등의 단위도 존재하나 CSS에서 사용하는 대표적인 크기 단위는 px, em, %이다. px은 절대값이고 em, %는 상대값이다. 대부분 브라우저의 폰트 사이즈 기본값은 16px, 1em, 100%이다. 프로퍼티 값이 0인 경우, 단위를 생략할 수 있다. 이 Box는 콘텐트(Content), 패딩(Padding), 테두리(Border), 마진(Margin)로 구성된다. 폰트와 텍스트에 관한 수많은 프로퍼티. position, z-index, overflow 프로퍼티. 정렬.